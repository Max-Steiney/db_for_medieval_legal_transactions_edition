# Verifikations-Report 2026-05-11

## Zusammenfassung

- Checks gesamt: 25
- Match: 9
- Mismatch: 0
- Bekannte Lücken: 10
- Info / Nicht direkt vergleichbar: 6

## Bekannte Lücken

### docs.total

- TEI: `2665`
- JSON: `2665`
- Notiz: Pipeline zählt zusätzlich Dokumente aus filenames.csv ohne TEI-Quelle (z. B. Vienna_1448-57_ready). Das Verifikations-Set rechnet nur auf TEI-Basis und deckt die Lücke auf.

### docs.by_collection

- TEI: `{QGW=2037, Stadtbuecher=628}`
- JSON: `{QGW=2037, Stadtbuecher=628}`
- Notiz: Pipeline zählt zusätzlich Dokumente aus filenames.csv ohne TEI-Quelle (z. B. Vienna_1448-57_ready). Das Verifikations-Set rechnet nur auf TEI-Basis und deckt die Lücke auf.

### persons.total_individual

- TEI: `16084`
- JSON: `8406`
- Notiz: personList.xml enthaelt das volle editorische Register; persons_search.json ist auf den freigegebenen Subset gefiltert (siehe knowledge/decisions.md, Personenregister-Freigabe).

### persons.by_sex

- TEI: `{m=11860, f=4224}`
- JSON: `{m=6082, f=2324}`
- Notiz: Selbe Wurzel wie persons.total_individual: TEI zaehlt das volle Register, JSON nur den freigegebenen Subset.

### roles.by_role_sex.issuer

- TEI: `{m=3211, f=1047}`
- JSON: `{m=2792, f=1002}`
- Notiz: Verifikation zaehlt rohe rs[@type='person']/@role-Annotationen in TEI; roles zaehlt aus persons_in_events.csv (Pipeline-CSV mit eigenen Scoping-Regeln). Differenz ist methodisch, kein Drift.

### roles.by_role_sex.none

- TEI: `{m=1123, f=331}`
- JSON: `{m=1660, f=291}`
- Notiz: Verifikation zaehlt rohe rs[@type='person']/@role-Annotationen in TEI; roles zaehlt aus persons_in_events.csv (Pipeline-CSV mit eigenen Scoping-Regeln). Differenz ist methodisch, kein Drift.

### roles.by_role_sex.other

- TEI: `{f=385, m=2224}`
- JSON: `{f=364, m=2025}`
- Notiz: Verifikation zaehlt rohe rs[@type='person']/@role-Annotationen in TEI; roles zaehlt aus persons_in_events.csv (Pipeline-CSV mit eigenen Scoping-Regeln). Differenz ist methodisch, kein Drift.

### roles.by_role_sex.recipient

- TEI: `{m=3234, f=1441}`
- JSON: `{m=2902, f=1395}`
- Notiz: Verifikation zaehlt rohe rs[@type='person']/@role-Annotationen in TEI; roles zaehlt aus persons_in_events.csv (Pipeline-CSV mit eigenen Scoping-Regeln). Differenz ist methodisch, kein Drift.

### roles.by_role_sex.witness

- TEI: `{m=4932, f=324}`
- JSON: `{m=4641, f=298}`
- Notiz: Verifikation zaehlt rohe rs[@type='person']/@role-Annotationen in TEI; roles zaehlt aus persons_in_events.csv (Pipeline-CSV mit eigenen Scoping-Regeln). Differenz ist methodisch, kein Drift.

### relations.by_type

- TEI: `{occ=5635, kin=5914, rep=419, friend=86, titel_ref=1, title=5, … (9 total)}`
- JSON: `{kin=5909, occ=5624, rep=419, friend=86}`
- Notiz: Pipeline normalisiert Beziehungstypen auf kin/occ/rep/friend. TEI enthält rohe @type-Werte inklusive Typo-Varianten (titel_ref, title, buis, ...). Counts daher nicht direkt vergleichbar.

## Matches

| Status | Name | Wert |
|---|---|---|
| OK | docs.date_range.QGW | `{min_date=1177-05-10, max_date=1414-12-20}` |
| OK | docs.date_range.Stadtbuecher | `{min_date=1388-07-13, max_date=1401-11-03}` |
| OK | provenance.roles.role_sex | `10766 file_keys geprüft` |
| OK | provenance.relations.drill_down | `12123 file_keys geprüft` |
| OK | provenance.relations.persons.rels | `12038 file_keys geprüft` |
| OK | provenance.transactions.drill_down | `1959 file_keys geprüft` |
| OK | provenance.register.persons | `15217 Doc-URLs geprüft` |
| OK | provenance.register.organisations | `4430 Doc-URLs geprüft` |
| OK | provenance.register.places | `38 Doc-URLs geprüft` |

## Info / Kontext-Aggregate ohne direkten Vergleich

### orgs.total_individual

- TEI: `1078`
- JSON: `—`
- Notiz: organisations_search.json nicht gebaut (Register noch nicht freigegeben).

### places.total_individual

- TEI: `2537`
- JSON: `—`
- Notiz: places_search.json nicht gebaut (Register noch nicht freigegeben).

### events.total

- TEI: `3323`
- JSON: `4214`
- Notiz: roles.coverage.total_events zählt Event-Nennungen (nicht dedupliziert); TEI-Zählung dedupliziert pro Dokument nach Event-Ref. Kein direkter Match zu erwarten.

### roles.by_role

- TEI: `{issuer=4258, other=2609, witness=5256, recipient=4675, none=1454, transactiongood_I=3}`
- JSON: `—`
- Notiz: Rollen-Counter aus TEI; kein direktes Pendant in JSON (nur role_by_sex).

### roles.by_role_sex.transactiongood_I

- TEI: `{m=3}`
- JSON: `—`
- Notiz: Pseudo-Rolle aus TEI-Annotationsfehlern; kein echter Rollen-Wert.

### transactions.disp_top

- TEI: `{verkaufen=353, verkauft=199, bestätigt=154, bestätigen=134, beurkundet=86, gibt=78, … (20 total)}`
- JSON: `—`
- Notiz: Top-20 Roh-Trigger für disp; Vergleich mit normalisierten Transaktionstypen in transactions noch offen.
